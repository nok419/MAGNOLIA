# deprecated content

削除せず互換性のために残す content が出た場合は、`content/gameplay/content-classification.json` の `deprecated` に列挙します。

ID を変更する場合は `content/gameplay/migrated-id-map.json` に旧 ID、新 ID、save への影響を書きます。
