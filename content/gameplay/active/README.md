# active content

現在の playable slice で使用する content は `content/gameplay/content-classification.json` の `active` に列挙します。

実ファイルは既存 loader との互換性を保つため、従来の `missions`, `enemies`, `bullet-patterns`, `projectiles` に置きます。
